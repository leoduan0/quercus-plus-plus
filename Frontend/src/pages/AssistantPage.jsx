export default function AssistantPage() {
  // TODO: chat UI with message bubbles
  // POST to backend /api/chat with { message, canvasData } on first message
  // Subsequent messages use { message, sessionId }
  return (
    <div className="page assistant-page">
      <p className="page-placeholder">AI Assistant — chat interface</p>
    </div>
  );
}
